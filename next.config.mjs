/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    // Probe branch only: allow runtime verification of the exact The Tree renderer.
    ignoreBuildErrors: true,
  },
};

export default nextConfig;
